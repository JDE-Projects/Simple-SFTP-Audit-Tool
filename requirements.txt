# Pinned build dependencies for Simple SFTP Audit Tool.
# Versions match the JDE pilot's green CI build (Python 3.14, 2026-06-17).
# Dependabot opens review-PRs for the == deps below when newer releases appear.
pywebview==6.2.1
PySide6==6.11.1
qtpy==2.4.3
pyinstaller==6.21.0

# ssh-audit is installed from PyPI now that upstream releases there again
# (current since v3.9.0, 2026-07-04). Dependabot opens review-PRs for it like
# the other == deps; review upstream changes before merging a bump. If PyPI
# ever goes stale again, see README "Keeping ssh-audit current" for the
# reviewed master-commit pin fallback.
ssh-audit==3.9.0
